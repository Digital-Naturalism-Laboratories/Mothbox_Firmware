Scripts that only existed in the old DIY (4.x) firmware tree and are not
called by anything. Kept for reference. They still hard-code the DIY relay
pins (26/20/21) and the old camera experiments; use the top-level scripts
(which dispatch through mothbox_hw.py) for anything you run on a live box.
